naftiko: 1.0.0-alpha2
info:
  label: Coresignal Multi-source Company API — Search
  description: 'Coresignal Multi-source Company API — Search. 2 operations. Lead operation: Search Companies (Elasticsearch
    DSL). Self-contained Naftiko capability covering one Coresignal business surface.'
  tags:
  - Coresignal
  - Search
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    CORESIGNAL_API_KEY: CORESIGNAL_API_KEY
capability:
  consumes:
  - type: http
    namespace: multi-source-company-search
    baseUri: https://api.coresignal.com/cdapi/v2/multi_source_company
    description: Coresignal Multi-source Company API — Search business capability. Self-contained, no shared references.
    resources:
    - name: search-es_dsl
      path: /search/es_dsl
      operations:
      - name: searchcompaniesbyesdsl
        method: POST
        description: Search Companies (Elasticsearch DSL)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: search-filter
      path: /search/filter
      operations:
      - name: searchcompaniesbyfilter
        method: POST
        description: Search Companies (filter)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: apikey
      value: '{{env.CORESIGNAL_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: multi-source-company-search-rest
    port: 8080
    description: REST adapter for Coresignal Multi-source Company API — Search. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/search/es-dsl
      name: search-es-dsl
      description: REST surface for search-es_dsl.
      operations:
      - method: POST
        name: searchcompaniesbyesdsl
        description: Search Companies (Elasticsearch DSL)
        call: multi-source-company-search.searchcompaniesbyesdsl
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/search/filter
      name: search-filter
      description: REST surface for search-filter.
      operations:
      - method: POST
        name: searchcompaniesbyfilter
        description: Search Companies (filter)
        call: multi-source-company-search.searchcompaniesbyfilter
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: multi-source-company-search-mcp
    port: 9090
    transport: http
    description: MCP adapter for Coresignal Multi-source Company API — Search. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: search-companies-elasticsearch-dsl
      description: Search Companies (Elasticsearch DSL)
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: multi-source-company-search.searchcompaniesbyesdsl
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: search-companies-filter
      description: Search Companies (filter)
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: multi-source-company-search.searchcompaniesbyfilter
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
